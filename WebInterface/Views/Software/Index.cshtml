@model IEnumerable<Data.Computers.SelectVMs.SoftwaresVM>

@{
    ViewData["Title"] = "Index";
}

<h1>@await Component.InvokeAsync("PageContentViewComponent", new { type = "SoftwareCategoryName" })</h1>


<div class="row">
    @if (Model != null && Model.Any())
    {
        foreach (var item in Model)
        {
                                    <div class="col-md mt-2">
                                        <div class="card">
                                            <div class="card-body">   
                                                <h5 class="card-title mb-2">@item.Name</h5>
                                                <p class="card-text">
                                                    <strong>@await Component.InvokeAsync("PageContentViewComponent", new { type = "SoftwarePublisherName" })</strong> <br />
                            @item.Publisher<br />
                                                    <strong>@await Component.InvokeAsync("PageContentViewComponent", new { type = "SoftwareLocationName" })</strong> <br />
                            @item.Location<br />
                                                    <strong>@await Component.InvokeAsync("PageContentViewComponent", new { type = "SoftwareStatusName" })</strong> <br />
                            @item.Status<br />                                                    
                                                </p>
                                                        <a class="navbar-brand" asp-area="" asp-controller="Software" asp-action="PrintToPDF" asp-route-id="@item.Id">
                            @await Component.InvokeAsync("PageContentViewComponent", new { type = "PrintToPDFButtonHeader" })
                                                            </a>
                                            </div>
                                        </div>
                                    </div>
        }
    }
    else
    {
                    <div class="col-md mt-2-4">
                        <div class="card mb-4">
                            <div class="card-body">
                                <h5 class="card-title mb-2">Brak obiektów do wyświetlenia</h5>
                            </div>
                        </div>
                    </div>
    }
</div>
